using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// Para los debufos probar a crear un sistema en el que se subscriba a estos y se desuscriba pasado los turnos, investigar como hacerlo
/// </summary>
public class BattleSystem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
